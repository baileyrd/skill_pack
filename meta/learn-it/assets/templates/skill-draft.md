<!--
Starting skeleton for step 3's draft. Fill every section from step 1's
actual reconstructed patterns — delete any section with nothing real
behind it rather than leaving placeholder prose. See
references/skill-authoring-conventions.md for the frontmatter/file-layout
rules and the description-quality bar this draft is held to.
-->
---
name: <lowercase-hyphenated-name>
description: <One long, dense paragraph. Name the domain explicitly. State what the skill does AND the mechanism it uses. List realistic trigger phrasings, including casual ones. Name companion/sibling skills if relevant. Err toward encouraging triggering over a narrow exact match.>
version: 1.0.0
---

# <Skill Name>

<One short paragraph: what this covers and the core mindset for working in
this domain — the thing you'd want a future Claude to internalize before
reading any further section.>

## Preferred patterns

<Approaches that worked this session, worth defaulting to next time. State
*why* each is preferred — a rule with no reason is weak guidance.>

## Anti-patterns

<Approaches tried and specifically rejected or corrected this session. Often
the most valuable section — these come from a real wrong turn, not
documentation.>

## Gotchas

<Non-obvious behavior that actually tripped up this session — surprising
defaults, version-specific quirks, edge cases documentation undersold.>

## Workflow / sequence

<Only if order genuinely mattered this session — the correct sequence for
setup/initialization/use. Omit this section entirely if nothing here was
order-dependent.>

## Quick reference

<Minimal, concrete examples pulled from this session — a real snippet,
command, or config, not an invented illustration.>

## Version notes

<If this guidance is version-specific to a library/tool/API, name the
version here so a future update knows when it's stale. Omit if not
applicable.>
